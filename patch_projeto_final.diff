diff --git a/include/linux/sched.h b/include/linux/sched.h
index 7bfc2b45c..7679cc98a 100644
--- a/include/linux/sched.h
+++ b/include/linux/sched.h
@@ -777,6 +777,7 @@ struct task_struct {
 	struct sched_entity		se;
 	struct sched_rt_entity		rt;
 	struct sched_dl_entity		dl;
+        struct list_head		bg_run_list;
 
 #ifdef CONFIG_SCHED_CORE
 	struct rb_node			core_node;
diff --git a/kernel/sched/Makefile b/kernel/sched/Makefile
index 978fcfca5..f7095455f 100644
--- a/kernel/sched/Makefile
+++ b/kernel/sched/Makefile
@@ -23,7 +23,7 @@ CFLAGS_core.o := $(PROFILING) -fno-omit-frame-pointer
 endif
 
 obj-y += core.o loadavg.o clock.o cputime.o
-obj-y += idle.o fair.o rt.o deadline.o
+obj-y += idle.o fair.o rt.o deadline.o background.o
 obj-y += wait.o wait_bit.o swait.o completion.o
 
 obj-$(CONFIG_SMP) += cpupri.o cpudeadline.o topology.o stop_task.o pelt.o
diff --git a/kernel/sched/core.c b/kernel/sched/core.c
index 2324b7055..1d3f85df6 100644
--- a/kernel/sched/core.c
+++ b/kernel/sched/core.c
@@ -4234,6 +4234,7 @@ static void __sched_fork(unsigned long clone_flags, struct task_struct *p)
 	p->se.nr_migrations		= 0;
 	p->se.vruntime			= 0;
 	INIT_LIST_HEAD(&p->se.group_node);
+        INIT_LIST_HEAD(&p->bg_run_list);
 
 #ifdef CONFIG_FAIR_GROUP_SCHED
 	p->se.cfs_rq			= NULL;
@@ -4413,6 +4414,8 @@ int sched_fork(unsigned long clone_flags, struct task_struct *p)
 		return -EAGAIN;
 	else if (rt_prio(p->prio))
 		p->sched_class = &rt_sched_class;
+        else if (p->policy == SCHED_BACKGROUND) /* <--- SUA ALTERAÇÃO AQUI */
+                p->sched_class = &background_sched_class;
 	else
 		p->sched_class = &fair_sched_class;
 
@@ -6795,6 +6798,8 @@ static void __setscheduler_prio(struct task_struct *p, int prio)
 		p->sched_class = &dl_sched_class;
 	else if (rt_prio(prio))
 		p->sched_class = &rt_sched_class;
+        else if (p->policy == SCHED_BACKGROUND) /* <--- SUA ALTERAÇÃO AQUI */
+                p->sched_class = &background_sched_class;
 	else
 		p->sched_class = &fair_sched_class;
 
@@ -9368,9 +9373,11 @@ void __init sched_init(void)
 	int i;
 
 	/* Make sure the linker didn't screw up */
-	BUG_ON(&idle_sched_class + 1 != &fair_sched_class ||
-	       &fair_sched_class + 1 != &rt_sched_class ||
-	       &rt_sched_class + 1   != &dl_sched_class);
+	BUG_ON(&idle_sched_class != &background_sched_class + 1 ||
+	       &background_sched_class != &fair_sched_class + 1 ||
+	       &fair_sched_class != &rt_sched_class + 1 ||
+	       &rt_sched_class   != &dl_sched_class + 1 ||
+	       &dl_sched_class   != &stop_sched_class + 1);
 #ifdef CONFIG_SMP
 	BUG_ON(&dl_sched_class + 1 != &stop_sched_class);
 #endif
@@ -9446,6 +9453,8 @@ void __init sched_init(void)
 		init_cfs_rq(&rq->cfs);
 		init_rt_rq(&rq->rt);
 		init_dl_rq(&rq->dl);
+                INIT_LIST_HEAD(&rq->bg.queue); /* Inicializando a fila BACKGROUND */
+                rq->bg.nr_running = 0;
 #ifdef CONFIG_FAIR_GROUP_SCHED
 		INIT_LIST_HEAD(&rq->leaf_cfs_rq_list);
 		rq->tmp_alone_branch = &rq->leaf_cfs_rq_list;
diff --git a/kernel/sched/sched.h b/kernel/sched/sched.h
index 5061093d9..3a12fd24e 100644
--- a/kernel/sched/sched.h
+++ b/kernel/sched/sched.h
@@ -180,7 +180,8 @@ static inline int dl_policy(int policy)
 static inline bool valid_policy(int policy)
 {
 	return idle_policy(policy) || fair_policy(policy) ||
-		rt_policy(policy) || dl_policy(policy);
+		rt_policy(policy) || dl_policy(policy) ||
+                policy == SCHED_BACKGROUND;
 }
 
 static inline int task_has_idle_policy(struct task_struct *p)
@@ -632,7 +633,6 @@ static inline int rt_bandwidth_enabled(void)
 #if defined(CONFIG_IRQ_WORK) && defined(CONFIG_SMP)
 # define HAVE_RT_PUSH_IPI
 #endif
-
 /* Real-Time classes' related field in a runqueue: */
 struct rt_rq {
 	struct rt_prio_array	active;
@@ -919,6 +919,11 @@ DECLARE_STATIC_KEY_FALSE(sched_uclamp_used);
  * (such as the load balancing or the thread migration code), lock
  * acquire operations must be ordered by ascending &runqueue.
  */
+struct bg_rq {
+    struct list_head queue;
+    unsigned int nr_running;
+};
+
 struct rq {
 	/* runqueue lock: */
 	raw_spinlock_t		__lock;
@@ -958,6 +963,7 @@ struct rq {
 	struct cfs_rq		cfs;
 	struct rt_rq		rt;
 	struct dl_rq		dl;
+        struct bg_rq		bg;
 
 #ifdef CONFIG_FAIR_GROUP_SCHED
 	/* list of leaf cfs_rq on this CPU: */
@@ -2226,6 +2232,7 @@ extern const struct sched_class stop_sched_class;
 extern const struct sched_class dl_sched_class;
 extern const struct sched_class rt_sched_class;
 extern const struct sched_class fair_sched_class;
+extern const struct sched_class background_sched_class; /* <--- ADICIONE */
 extern const struct sched_class idle_sched_class;
 
 static inline bool sched_stop_runnable(struct rq *rq)
